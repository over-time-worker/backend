spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5431/overtime
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create